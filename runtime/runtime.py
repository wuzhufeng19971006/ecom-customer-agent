"""Agent Runtime 统一入口。

整合 conversation / decision / planner / memory / multimodal / llm / workflow。
"""
from __future__ import annotations


class AgentRuntime:
    """Agent Runtime 入口类（预留接口）。

    TODO: 后续整合所有 runtime 子模块，提供统一的 run() 入口
    """
    pass
