# 电商客服 Agent 平台 — 方案架构

> 项目：`ecom-customer-agent`  
> 版本：0.1.0  
> 定位：面向淘宝 / 拼多多 / 抖店的智能客服与多 Agent 平台骨架  
> 文档更新：2026-07-22（对齐企业级多包重构后结构）

---

## 1. 项目概述

### 1.1 业务目标

构建一套**可多应用扩展**的电商智能体平台，一期聚焦客服：

| 能力 | 说明 | 当前状态 |
|------|------|----------|
| FAQ / 政策 / 商品知识答疑 | 严格基于知识库 RAG，少幻觉 | ✅ 可用 |
| 多模态咨询（截图 + 文字） | Vision + OCR → 查询改写 → RAG | ✅ 可用 |
| 订单 / 物流 / 商品实时查询 | Function Calling + 平台适配器 | 🔶 工具与循环已通，平台 API 占位 |
| 转人工 | 关键词兜底 + LLM 工具调用 | ✅ 逻辑可用 |
| 千牛 / 平台 Webhook 接入 | 消息入 → Agent → 回复 | 🔶 结构就绪，签名与真实 API 待接 |
| 敏感信息脱敏 | 手机号 / 订单号 / 身份证等 | ✅ Masker 实现；主路径接入待做 |
| 工具中心 / MCP / 多应用 | 企业级扩展 | ⬜ 目录与接口预留，大量 stub |

### 1.2 设计原则

1. **平台无关内核**：Agent 只依赖 `PlatformAdapter` 抽象，不感知具体电商平台。  
2. **严格 RAG 优先**：答疑路径强制「只基于检索片段回答」，无匹配则转人工话术。  
3. **多模态事件驱动**：图片上传不立即强答；经 Vision/OCR 后由决策引擎决定 ANSWER / WAIT / BEST_EFFORT。  
4. **Provider 可替换**：LLM / VLM / Embedding / Rerank 均抽象为接口。  
5. **多包分层演进**：业务在 `apps/`，运行时在 `runtime/`，知识在 `knowledge_platform/`，工具在 `tool_center/`，安全在 `security/`；旧 `app/` 仅作兼容 re-export。

---

## 2. 技术选型

| 层次 | 技术 | 用途 |
|------|------|------|
| 运行时 | Python ≥ 3.10 | 异步业务逻辑 |
| Web 框架 | FastAPI + Uvicorn | HTTP API / Webhook |
| 配置 | pydantic-settings + `.env` | 集中管理密钥与阈值 |
| 日志 | structlog | 结构化日志 |
| 关系库 | SQLAlchemy 2.x async + aiosqlite | 会话 / 消息 / 转人工任务（schema 已备） |
| 向量库 | ChromaDB（本地持久化） | FAQ / 商品 / 政策知识检索 |
| 生成 LLM | DeepSeek（`deepseek-chat`） | RAG 生成、查询改写、Agent 工具循环 |
| 视觉 LLM | 小米 MiMo-V2.5 | 截图理解、结构化 Vision、OCR |
| Embedding | 阿里云 DashScope `text-embedding-v3` | 知识写入与查询向量化 |
| Rerank | Jina Reranker v2 multilingual | 召回二次精排 |
| HTTP 客户端 | httpx + tenacity | 外部 API 调用与重试 |
| 测试 | pytest + pytest-asyncio | 冒烟 / 多模态 / 工具 / 脱敏等 |

---

## 3. 总体架构（重构后）

```
                         ┌──────────────────────────────────────┐
                         │           统一入口 main.py            │
                         │  uvicorn factory → customer_service   │
                         └──────────────────┬───────────────────┘
                                            │
                         ┌──────────────────▼───────────────────┐
                         │     apps/customer_service_agent       │
                         │  API · Agent · Adapters · CLI         │
                         └─┬────────────┬────────────┬──────────┘
                           │            │            │
              ┌────────────▼──┐  ┌──────▼──────┐  ┌─▼──────────────┐
              │   runtime/    │  │ knowledge_  │  │  tool_center/  │
              │ conversation  │  │  platform/  │  │ registry       │
              │ decision      │  │ service     │  │ executor       │
              │ multimodal    │  │ retriever   │  │ mcp (stub)     │
              │ llm providers │  │ ingest      │  └────────────────┘
              └───────┬───────┘  └──────┬──────┘
                      │                 │
         ┌────────────▼─────────────────▼────────────┐
         │              common/ · security/            │
         │   config · logger · database · masker       │
         └─────────────────────────────────────────────┘
                      │
         ┌────────────▼───────────────────────────────┐
         │  data/chroma · SQLite · 外部 LLM/平台 API   │
         └────────────────────────────────────────────┘
```

### 3.1 包职责一览

| 包 | 职责 | 落地程度 |
|----|------|----------|
| `apps/customer_service_agent` | 客服应用：API、AgentLoop、QA、淘宝适配 | ✅ 主路径可用 |
| `apps/operation_agent` 等 | 运营 / 卖家 / 管理台 | ⬜ 空壳 |
| `runtime` | 会话编排、决策、多模态、LLM Provider、规划/工作流接口 | ✅ 核心可用；Workflow/AgentRuntime 预留 |
| `knowledge_platform` | RAG 流水线、Chroma 检索、灌库；知识中心全生命周期目录 | ✅ 检索可用；Center 子模块多为 stub |
| `tool_center` | 工具注册表、执行器、权限/MCP 接口 | 🔶 Registry/Executor 可用，未完全接入 AgentLoop |
| `mcp_services` | 订单/物流/商品等 MCP 服务 | ⬜ 空壳 |
| `security` | 脱敏、RBAC、Auth、审计、租户 | 🔶 Masker 实现完整；其余预留 |
| `common` | 配置、日志、DB ORM、缓存/工具 | ✅ config/logger/database 可用 |
| `platform_infra` | 配置中心、监控、Prompt 中心等 | ⬜ 空壳 |
| `app/` | **兼容层**（re-export 到新包） | 过渡期保留，新代码勿再写入 |

---

## 4. 目录结构

```
ecom-customer-agent/
├── main.py                              # 统一启动：factory → customer_service_agent
├── pyproject.toml                       # packages: app/apps/runtime/tool_center/...
├── .env.example
├── data/
│   ├── faq.jsonl
│   └── chroma/
│
├── apps/
│   └── customer_service_agent/          # 一期唯一业务应用
│       ├── main.py                      # FastAPI create_app
│       ├── api/                         # /api/qa, /api/qa/multimodal, /webhooks/taobao
│       ├── agent/                       # QAService, AgentLoop, Session, tools
│       ├── adapters/                    # PlatformAdapter + taobao
│       └── cli/                         # 本地答疑调试
│
├── runtime/
│   ├── conversation/                    # SessionManager, State, Event, ContextBuilder
│   ├── decision/                        # DecisionEngine
│   ├── multimodal/                      # Vision / OCR / ImageAnalyzer
│   ├── llm/                             # 抽象 + providers (deepseek/mimo/dashscope/jina)
│   ├── planner/                         # TaskPlanner（预留）
│   ├── memory/                          # MemoryStore（预留）
│   ├── workflow/                        # WorkflowEngine 等（预留）
│   └── runtime.py                       # AgentRuntime 统一入口（预留）
│
├── knowledge_platform/
│   ├── knowledge_service/               # RAGPipeline, retriever, ingest, citation...
│   ├── knowledge_center/                # 上传/解析/切分/审核/发布（目录预留）
│   ├── providers/                       # rag / graph / hybrid（预留）
│   └── evaluation|governance|feedback   # 评测与治理（预留）
│
├── tool_center/
│   ├── registry/                        # ToolRegistry
│   ├── manager/                         # ToolExecutor, selector, parser
│   ├── permission/                      # 工具策略（预留）
│   ├── mcp/                             # MCP client/server adapter（预留）
│   └── builtin_tools/                   # 示例工具（预留）
│
├── security/
│   ├── data_mask/                       # Masker ✅
│   ├── rbac/ auth/ audit_log/ tenant/   # 预留
│   └── sensitive_detector/              # 预留
│
├── common/
│   ├── config/ logger/ database/        # ✅
│   └── cache/ exceptions/ utils/        # 部分预留
│
├── mcp_services/                        # order/logistics/product... 空壳
├── platform_infra/                      # 配置中心/监控等 空壳
├── app/                                 # 旧路径兼容 re-export
└── tests/                               # 已对齐新 import 路径
```

---

## 5. 核心数据流

### 5.1 纯文本 FAQ（`POST /api/qa`）

```
用户问题
  → apps...agent.qa.QAService
  → knowledge_platform...RAGPipeline.retrieve(collections=["kb_faq"])
  → Chroma top_k + Jina top_n
  → DeepSeek（严格 System Prompt）
  → { answer, matched, sources }
```

### 5.2 多模态（`POST /api/qa/multimodal`）

```
multipart: text? + image? + session_id?
  → runtime.conversation.ConversationManager
       ├─ ingest_image → ImageAnalyzer (Vision ∥ OCR → filter)
       ├─ ingest_text  → 500ms debounce
       └─ DecisionEngine → ANSWER | WAIT | BEST_EFFORT
  → ContextBuilder.rewrite
  → QAService.answer
  → EventBus ANSWER_READY
```

**会话状态：** `COLLECTING` → `PROCESSING` → `READY` / `WAITING_USER` → `ANSWERED`

### 5.3 Webhook + 工具 Agent（`POST /webhooks/taobao`）

```
千牛 payload
  → TaobaoAdapter.parse_incoming
  → Session（当前每次新建 UUID，待改为按买家恢复）
  → AgentLoop：关键词兜底 → RAG → LLM+tools 循环
       tools: query_order / query_logistics / query_product / handoff_human
  → adapter.send_message（占位）
```

### 5.4 知识灌库

```bash
python -m knowledge_platform.knowledge_service.ingest \
  --source data/faq.jsonl --collection kb_faq [--clear]
```

集合：`kb_faq` / `kb_product` / `kb_policy`

---

## 6. 模块设计要点

### 6.1 应用层（`apps/customer_service_agent`）

- **QAService**：仅 FAQ 集合 + 低温度 + 禁止编造。  
- **AgentLoop**：RAG 上下文 + Function Calling（最多 4 步）+ 关键词转人工。  
- **PlatformAdapter**：统一 `send_message` / `query_*`；淘宝实现仍为占位。

### 6.2 Runtime

| 子模块 | 关键类型 | 说明 |
|--------|----------|------|
| conversation | `ConversationManager`, `ConversationState`, `EventBus`, `ContextBuilder` | 多轮与多模态编排 |
| decision | `DecisionEngine` | ANSWER / WAIT / BEST_EFFORT |
| multimodal | `VisionService`, `OCRService`, `ImageAnalyzer` | MiMo 结构化视觉 + OCR 过滤 |
| llm | `LLMProvider` 等 + DeepSeek/MiMo/DashScope/Jina | Provider 抽象与实现 |
| planner / memory / workflow | 预留接口 | 复杂任务与长期记忆未落地 |

### 6.3 Knowledge Platform

- **可用：** `RAGPipeline`、`ChromaStore`、`ingest`、query rewriter re-export。  
- **预留：** knowledge_center 全生命周期、GraphRAG、评测与治理。

### 6.4 Tool Center

- **可用：** `ToolRegistry`、`ToolExecutor`（含批量 tool_calls）。  
- **默认注册：** 客服四工具（从 `apps...agent.tools` 延迟导入）。  
- **缺口：** `AgentLoop` 仍直接 `execute_tool()`，未强制走 ToolExecutor/权限。

### 6.5 Security

- **Masker：** 手机 / 订单 / 身份证 / 银行卡 / 邮箱 / 门牌；`mask` + `restore`。  
- **未接线：** QA / AgentLoop / Conversation 主路径尚未默认脱敏。  
- **RBAC / Auth / Audit / Tenant：** 接口预留。

### 6.6 Common

- `Settings`：模型密钥、会话 debounce/timeout、淘宝、DB、Chroma。  
- `database`：`sessions` / `messages` / `handoff_tasks` ORM（主路径仍多为内存会话）。

---

## 7. API 一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/api/qa` | 纯文本 FAQ |
| POST | `/api/qa/multimodal` | multipart 多模态 / 多轮 |
| POST | `/webhooks/taobao` | 千牛回调（AgentLoop） |

启动：

```bash
# 推荐
python main.py
# 或
uvicorn apps.customer_service_agent.main:create_app --factory --host 0.0.0.0 --port 8000
```

CLI：

```bash
python -m apps.customer_service_agent.cli.qa "你们的尺码准吗"
python -m apps.customer_service_agent.cli.qa --interactive
```

---

## 8. 配置（环境变量）

| 类别 | 变量前缀 | 说明 |
|------|----------|------|
| 应用 | `APP_*`, `LOG_LEVEL` | 环境、端口、日志 |
| 生成 | `DEEPSEEK_*` | RAG / 改写 / Agent |
| 视觉 | `MIMO_*` | 截图理解 + OCR |
| 向量 | `DASHSCOPE_*` | Embedding |
| 精排 | `JINA_*` | Reranker |
| 淘宝 | `TAOBAO_*` | 开放平台与 Webhook |
| 数据 | `DATABASE_URL`, `CHROMA_PERSIST_DIR` | SQLite + Chroma |
| 会话 | debounce 500ms / vision 阈值 0.85 / timeout 30s | 见 `common.config` |

---

## 9. 部署与运行

```bash
pip install -e ".[dev]"
cp .env.example .env   # 填入密钥

python -m knowledge_platform.knowledge_service.ingest \
  --source data/faq.jsonl --collection kb_faq --clear

python main.py
# pytest
python -m pytest tests/ -q
```

**多实例注意：** `ConversationManager` / EventBus / 部分 client 为进程内单例；生产需会话落库 + 共享存储。

---

## 10. 已知缺口与风险（重构后）

| 风险 | 影响 | 建议 |
|------|------|------|
| 淘宝 Adapter 占位 | 无法真实查单/回消息 | 接入 TOP 签名与消息 API |
| Webhook 无验签 | 安全风险 | 实现签名校验 |
| 会话内存 + 每次新 UUID | 无多轮、难扩容 | 落库 + 按 buyer/shop 恢复 |
| Masker 未进主路径 | 敏感信息可能进 LLM | QA/Loop 入口统一 mask/restore |
| ToolCenter 未接管 AgentLoop | 权限/审计难统一 | Loop 改走 ToolExecutor |
| 大量 stub 包 | 名实不符、维护成本 | 有计划再填，无计划可删目录 |
| `app/` 双轨 | 改错包风险 | 新代码只写新包；兼容层设下线期限 |
| `matched` 启发式 | 边界误判 | JSON 结构化输出或独立 judge |

---

## 11. 演进蓝图

```
Channel Gateway (淘宝/拼多多/抖店/自建 IM)
        ↓
API Gateway + Auth/RBAC
        ↓
Agent Runtime Kernel (统一 run：Event · Session · Decision · Planner)
        ↓
   Multimodal │ Knowledge │ Tool Center │ LLM Router
        ↓
Infra: Postgres · Redis · MQ · OSS · Observability
```

**建议优先级：**

1. ~~打包与测试对齐新架构~~ ✅（2026-07-22）  
2. 淘宝真实收发 + Webhook 验签 + 会话持久化  
3. Masker / 审计接入主路径；AgentLoop 走 ToolCenter  
4. 充实商品/政策知识与评测集  
5. 按真实需求填充 mcp_services / knowledge_center，避免空壳膨胀  
6. 多平台 Adapter 与水平扩展  

---

## 12. 小结

重构后项目从**单体 `app/`** 演进为：

> **`apps`（业务）+ `runtime`（编排）+ `knowledge_platform`（知识）+ `tool_center`（工具）+ `security`（安全）+ `common`（基础设施）**

- **当下可跑：** FAQ 严格答疑、多模态编排、工具循环骨架、脱敏算法、灌库、CLI、测试（新路径）。  
- **结构就绪、待填：** MCP 服务矩阵、知识中心流水线、统一 AgentRuntime、多应用、平台基建。  
- **下一里程碑：** 平台真实 API、会话持久化、安全主路径、工具中心真正接管 Agent。

本文档以**当前仓库真实目录与代码状态**为准；空壳模块在状态列标为预留，避免与已实现能力混淆。
