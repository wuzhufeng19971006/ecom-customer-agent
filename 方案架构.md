# 电商客服 Agent 平台 — 方案架构

> 项目：`ecom-customer-agent`  
> 版本：0.3.0  
> 定位：面向抖店（抖音电商）的智能客服 Agent 系统  
> 文档更新：2026-07-31（安全加固 + Bug 清零 + 会话持久化 + 共享引擎 + 公共 API 封装）

---

## 1. 项目概述

### 1.1 业务目标

为面部彩妆 / 画笔颜料等品类的抖店商家提供 AI 客服，替代每月 2500 元外包客服成本：

| 能力 | 说明 | 当前状态 |
|------|------|----------|
| FAQ / 政策 / 商品知识答疑 | 严格基于知识库 RAG，少幻觉 | ✅ 可用 |
| 多模态咨询（截图 + 文字） | Vision + OCR → 查询改写 → RAG | ✅ 可用 |
| 订单 / 物流 / 商品实时查询 | Function Calling + 抖店适配器 | 🔶 工具循环已通，抖店 API 签名已实现，待真实 token 验证 |
| 转人工 | 关键词兜底 + LLM 工具调用 | ✅ 逻辑可用 |
| 抖店 Webhook 接入 | 消息入 → Agent → 回复 | 🔶 结构就绪，签名校验已实现，待真实推送验证 |
| 敏感信息脱敏 | 手机号 / 订单号 / 身份证等 | ✅ Masker 全路径接入（用户输入 + 工具结果 + LLM 上下文 + DB 落库） |
| 会话持久化 | 按 buyer_id + platform + shop_id 恢复多轮对话 | ✅ DB 持久化 + 内存 LRU 缓存 + TTL 过期 |
| 管理后台 | 知识库 CRUD / 会话管理 / 测试问答 / 批量测试 / 仪表盘 | ✅ 可用 |
| 淘宝兼容 | 保留适配器与 Webhook，含 HMAC-SHA256 签名校验 | 🔶 适配器占位，验签已实现 |

### 1.2 设计原则

1. **平台无关内核**：Agent 只依赖 `PlatformAdapter` 抽象，不感知具体电商平台。  
2. **严格 RAG 优先**：答疑路径强制「只基于检索片段回答」，支持语义等价问题匹配，无匹配则转人工话术。  
3. **多模态事件驱动**：图片上传不立即强答；经 Vision/OCR 后由决策引擎决定 ANSWER / WAIT / BEST_EFFORT。  
4. **Provider 可替换**：LLM / VLM / Embedding / Rerank 均抽象为接口。  
5. **单包分层**：业务在 `apps/`，运行时在 `runtime/`，知识在 `knowledge_platform/`，工具在 `tool_center/`，安全在 `security/`，基础设施在 `common/`。

---

## 2. 技术选型

| 层次 | 技术 | 用途 |
|------|------|------|
| 运行时 | Python ≥ 3.10 | 异步业务逻辑 |
| Web 框架 | FastAPI + Uvicorn | HTTP API / Webhook / 管理后台 |
| 配置 | pydantic-settings + `.env` | 集中管理密钥与阈值 |
| 日志 | structlog | 结构化日志 |
| 关系库 | SQLAlchemy 2.x async + aiosqlite | 会话 / 消息 / 转人工任务 |
| 向量库 | ChromaDB（本地持久化） | FAQ / 商品 / 政策知识检索 |
| 生成 LLM | DeepSeek V4（`deepseek-v4-flash`） | RAG 生成、查询改写、Agent 工具循环 |
| 视觉 LLM | 小米 MiMo-V2.5 | 截图理解、结构化 Vision、OCR |
| Embedding | 阿里云 DashScope `text-embedding-v3` | 知识写入与查询向量化 |
| Rerank | Jina Reranker v2 multilingual | 召回二次精排 |
| HTTP 客户端 | httpx + tenacity | 外部 API 调用与重试 |
| 测试 | pytest + pytest-asyncio | 56 项测试（冒烟 / 多模态 / 工具 / 脱敏 / 抖店适配器 / RAG） |

---

## 3. 总体架构

```
                         ┌──────────────────────────────────────┐
                         │           统一入口 main.py            │
                         │  uvicorn factory → customer_service   │
                         └──────────────────┬───────────────────┘
                                            │
                         ┌──────────────────▼───────────────────┐
                         │     apps/customer_service_agent       │
                         │  API · Agent · Adapters · CLI · Admin │
                         └─┬────────────┬────────────┬──────────┘
                           │            │            │
              ┌────────────▼──┐  ┌──────▼──────┐  ┌─▼──────────────┐
              │   runtime/    │  │ knowledge_  │  │  tool_center/  │
              │ conversation  │  │  platform/  │  │ registry       │
              │ decision      │  │ service     │  │ executor       │
              │ multimodal    │  │ retriever   │  └────────────────┘
              │ llm providers │  │ ingest      │
              └───────┬───────┘  └────┬────────┘
                      │               │
         ┌────────────▼───────────────▼────────────┐
         │              common/ · security/         │
         │   config · logger · database · masker    │
         └──────────────────────────────────────────┘
                      │
         ┌────────────▼───────────────────────────────┐
         │  data/chroma · SQLite · 外部 LLM/平台 API   │
         └────────────────────────────────────────────┘
```

### 3.1 包职责一览

| 包 | 职责 | 落地程度 |
|----|------|----------|
| `apps/customer_service_agent` | 客服应用：API、AgentLoop、QA、抖店适配器、管理后台 | ✅ 主路径可用 |
| `runtime` | 会话编排、决策、多模态、LLM Provider | ✅ 核心可用 |
| `knowledge_platform` | RAG 流水线、Chroma 检索、灌库 | ✅ 检索可用；评测/治理/调度预留 |
| `tool_center` | 工具注册表、执行器 | ✅ Registry/Executor 可用，AgentLoop 已通过 ToolExecutor 执行工具 |
| `security` | 脱敏、RBAC | ✅ Masker 全路径接入；RBAC 接口预留 |
| `common` | 配置、日志、DB ORM、工具 | ✅ 全部可用 |

> 已移除的包：`app/`（旧兼容层）、`platform_infra/`、`mcp_services/`、`knowledge_platform/knowledge_center/`。不再保留空壳目录，减少维护负担。

---

## 4. 目录结构

```
ecom-customer-agent/
├── main.py                              # 统一启动：factory → customer_service_agent
├── pyproject.toml                       # packages: apps/runtime/tool_center/...
├── .env.example
├── 方案架构.md
│
├── data/
│   ├── faq.jsonl                        # 初始 FAQ 数据
│   ├── app.db                           # SQLite（会话/消息/转人工任务）
│   └── chroma/                          # ChromaDB 持久化
│
├── apps/
│   └── customer_service_agent/          # 一期唯一业务应用
│       ├── main.py                      # FastAPI create_app + lifespan
│       ├── api/
│       │   ├── qa.py                    # POST /api/qa（纯文本 FAQ）
│       │   ├── qa_multimodal.py         # POST /api/qa/multimodal（图片+文本）
│       │   ├── webhooks.py              # POST /webhooks/doudian + /webhooks/taobao
│       │   ├── admin.py                 # 管理后台 API（知识/会话/统计 CRUD，共享 DB 引擎）
│       │   ├── admin_ui.py              # 管理后台 SPA（HTML 单页应用，5 页面）
│       │   └── batch_test.py            # 批量测试 API（Excel 上传/后台执行/审核/导出）
│       ├── agent/
│       │   ├── answer_engine.py         # AnswerEngine（统一脱敏/RAG/LLM/工具循环引擎）
│       │   ├── qa.py                    # QAService（委托 AnswerEngine.answer_qa）
│       │   ├── loop.py                  # AgentLoop（委托 AnswerEngine.answer_agent）
│       │   ├── session.py               # 会话状态（含 MAX_HISTORY 截断）
│       │   ├── session_store.py         # 会话持久化（内存 LRU + DB，按 buyer/platform/shop 恢复）
│       │   ├── query_rewrite.py         # 查询改写
│       │   └── tools/                   # 客服四工具定义与 ToolExecutor 注册
│       ├── adapters/
│       │   ├── base.py                  # PlatformAdapter 抽象基类
│       │   ├── doudian/adapter.py       # 抖店适配器（签名+API 已实现）
│       │   └── taobao/adapter.py        # 淘宝适配器（占位保留）
│       └── cli/                         # 本地答疑调试
│
├── runtime/
│   ├── conversation/
│   │   ├── session_manager.py           # ConversationManager（debounce + 事件驱动）
│   │   ├── state.py                     # 会话状态机
│   │   ├── event.py                     # EventBus
│   │   └── context_builder.py           # 多模态查询改写
│   ├── decision/
│   │   └── decision_engine.py           # ANSWER / WAIT / BEST_EFFORT
│   ├── multimodal/
│   │   ├── vision_service.py            # MiMo-V2.5 结构化视觉（6 字段 JSON）
│   │   ├── ocr_service.py               # OCR + 过滤
│   │   └── image_analyzer.py            # Vision ∥ OCR 编排
│   ├── llm/
│   │   ├── llm_provider.py              # LLM/VLM/Embedding/Rerank 抽象基类
│   │   ├── model_router.py              # 模型路由
│   │   └── providers/
│   │       ├── deepseek.py              # DeepSeek V4 Flash
│   │       ├── mimo.py                  # MiMo-V2.5 Vision
│   │       ├── embedding_dashscope.py   # DashScope text-embedding-v3
│   │       └── rerank_jina.py           # Jina Reranker v2
│   └── __init__.py
│
├── knowledge_platform/
│   ├── knowledge_service/
│   │   ├── service.py                   # RAGPipeline（召回 → Jina 精排 → 上下文）
│   │   ├── ingest.py                    # JSONL 批量灌库
│   │   ├── retriever/retriever.py       # ChromaStore + RetrievalHit
│   │   ├── reranker/reranker.py         # Reranker re-export
│   │   └── query_rewriter/query_rewriter.py  # 查询改写
│   ├── evaluation/                      # 评测（预留）
│   ├── feedback/                        # 反馈（预留）
│   ├── governance/                      # 治理（预留）
│   ├── scheduler/                       # 调度（预留）
│   └── providers/                       # rag/graph_rag/hybrid/llmwiki（预留）
│
├── tool_center/
│   ├── registry/
│   │   ├── tool_registry.py             # ToolRegistry
│   │   └── tool_metadata.py             # 工具元数据
│   └── manager/
│       └── tool_executor.py             # ToolExecutor（含批量 tool_calls）
│
├── security/
│   ├── data_mask/masker.py              # 脱敏（手机/订单/身份证/银行卡/邮箱/门牌）✅
│   └── rbac/rbac.py                     # RBAC 角色权限（接口预留）
│
├── common/
│   ├── config/config.py                 # Settings 全局配置
│   ├── logger/logger.py                 # structlog 日志
│   ├── database/database.py             # SQLAlchemy ORM（SessionRow/MessageRow/HandoffTask）
│   ├── exceptions/exceptions.py         # 自定义异常
│   └── utils/utils.py                   # 通用工具
│
└── tests/                               # 56 项测试
    ├── test_smoke.py                    # 冒烟测试（5 项）
    ├── test_multimodal.py               # 多模态（10 项）
    ├── test_tools.py                    # 工具注册与执行（8 项）
    ├── test_masker.py                   # 脱敏（14 项）
    ├── test_doudian_adapter.py          # 抖店适配器（14 项）
    └── test_runtime_rag_llm.py          # RAG/LLM 集成（5 项）
```

---

## 5. 核心数据流

### 5.1 纯文本 FAQ（`POST /api/qa`）

```
用户问题
  → apps...agent.qa.QAService
  → knowledge_platform...RAGPipeline.retrieve(collections=["kb_faq"])
  → Chroma top_k + Jina top_n
  → DeepSeek V4 Flash（严格 System Prompt，支持语义等价匹配）
  → { answer, matched, sources }
```

**Prompt 策略：** System Prompt 明确要求识别语义等价问题（如"下单后多久到"="发货时间"），仅对完全无关的问题返回"无法回答"。

### 5.2 多模态（`POST /api/qa/multimodal`）

```
multipart: text? + image? + session_id?
  → runtime.conversation.ConversationManager
       ├─ ingest_image → ImageAnalyzer (Vision ∥ OCR → filter)
       ├─ ingest_text  → 500ms debounce
       └─ DecisionEngine → ANSWER | WAIT | BEST_EFFORT
  → ContextBuilder.rewrite
  → QAService.answer
  → EventBus ANSWER_READY
```

**会话状态：** `COLLECTING` → `PROCESSING` → `READY` / `WAITING_USER` → `ANSWERED`

**Vision 输出结构（6 字段 JSON）：** `page_type` / `focus_area` / `detected_text` / `user_annotation` / `possible_intent` / `confidence`

### 5.3 Webhook + 工具 Agent（`POST /webhooks/doudian`）

```
抖店推送 payload
  → DoudianAdapter.parse_incoming（含 MD5 签名校验，验签失败返回 403）
  → SessionStore.get_or_create（按 platform + buyer_id + shop_id 恢复会话）
  → 脱敏用户消息 → 持久化到 DB（脱敏文本）
  → AgentLoop → AnswerEngine.answer_agent：
       ├─ 脱敏（Masker） → 关键词兜底 → RAG 全集合检索
       ├─ LLM + ToolExecutor 工具循环（最多 4 步）
       │    tools: query_order / query_logistics / query_product / handoff_human
       │    工具参数 restore 真实值 → 执行 → 结果 mask 后再入 session
       └─ 回复 restore 真实值 → 持久化脱敏版到 DB
  → DoudianAdapter.send_message（调用 im.sendMsg 回复买家）
```

**安全要点：**
- 用户输入 → 脱敏后进 LLM 和 DB
- 工具参数 → restore 真实值调用平台 API → 结果立即 mask 再存入会话历史
- 助手回复 → 发送真实版（restore），DB 存脱敏版（masked_answer）
- Session 恢复增加 `shop_id` 过滤，防止跨店铺会话串用
- 淘宝 Webhook 同样实现了 HMAC-SHA256 签名校验

### 5.4 知识灌库

```bash
python -m knowledge_platform.knowledge_service.ingest \
  --source data/faq.jsonl --collection kb_faq [--clear]
```

集合：`kb_faq` / `kb_product` / `kb_policy`

也可通过管理后台 `/admin/` 在线录入或批量导入。

---

## 6. 模块设计要点

### 6.1 应用层（`apps/customer_service_agent`）

- **AnswerEngine**：统一回答引擎，三条路径（QA / Agent / Multimodal）共享核心逻辑：
  - 脱敏（Masker）— 用户输入和工具结果均在进入 LLM 前脱敏
  - RAG 召回 — 全集合检索（kb_faq / kb_product / kb_policy）
  - LLM 调用 — JSON 结构化输出（QA 模式）/ Function Calling 工具循环（Agent 模式）
  - 解析 — JSON parse + 启发式回退
  - 恢复脱敏 — reply 恢复真实值发给用户，masked_answer 保留占位符落库
- **QAService**：委托 AnswerEngine.answer_qa，仅 FAQ 集合 + 低温度（0.1）+ 禁止编造 + 语义等价匹配。  
- **AgentLoop**：委托 AnswerEngine.answer_agent，RAG 上下文 + ToolExecutor 工具循环（最多 4 步）+ 关键词转人工兜底。
- **SessionStore**：内存 LRU 缓存 + DB 持久化，按 `(platform, buyer_id, shop_id)` 三元组恢复会话，TTL 30 分钟，缓存上限 500。  
- **DoudianAdapter**：完整实现抖店开放平台接入：
  - HMAC-SHA256 签名（`_sign`）
  - 消息推送验签（`_verify_push_sign`，MD5）
  - 统一 API 调用（`_call_doudian`）
  - 消息收发（`parse_incoming` / `send_message`）
  - 业务查询（`query_order` / `query_logistics` / `query_product`）
- **TaobaoAdapter**：HMAC-SHA256 签名校验已实现，业务查询方法占位保留。  
- **管理后台**：
  - API（`admin.py`）：知识 CRUD + 批量导入（通过 ChromaStore 公共 API）、会话 CRUD（JOIN 批量查询，消除 N+1）、统计
  - API（`batch_test.py`）：Excel 上传解析 + 后台异步批量测试 + 人工审核 + 结果导出
  - UI（`admin_ui.py`）：5 页面 SPA（仪表盘 / 知识库 / 会话记录 / 测试问答 / 批量测试）
  - 共享 DB 引擎：`_get_db()` 惰性单例，避免每个请求创建/销毁连接池

### 6.2 Runtime

| 子模块 | 关键类型 | 说明 |
|--------|----------|------|
| conversation | `ConversationManager`, `ConversationState`, `EventBus`, `ContextBuilder` | 多轮与多模态编排，500ms debounce |
| decision | `DecisionEngine` | ANSWER / WAIT / BEST_EFFORT 三态决策 |
| multimodal | `VisionService`, `OCRService`, `ImageAnalyzer` | MiMo 结构化视觉（6 字段 JSON）+ OCR 过滤 |
| llm | `LLMProvider` / `VLMProvider` / `EmbeddingProvider` / `RerankProvider` + 4 个实现 | Provider 抽象与实现 |

### 6.3 Knowledge Platform

- **可用：** `RAGPipeline`（召回 → Jina 精排）、`ChromaStore`（含公共 CRUD API：`get_documents` / `update_document` / `delete_document`）、`ingest`、query rewriter。  
- **ChromaDB 配置：** collection 显式设置 `hnsw:space=cosine`，确保 `1.0 - distance` 正确计算余弦相似度。  
- **Jina Reranker 容错：** `document` 字段同时处理 `str` 和 `dict` 两种响应格式；embedding 强命中兜底——若 top-1 被 reranker 挤出则强制补到最前。  
- **预留：** evaluation / feedback / governance / scheduler / providers（graph_rag / hybrid / llmwiki）。

### 6.4 Tool Center

- **可用：** `ToolRegistry`、`ToolExecutor`（含批量 tool_calls）。  
- **AgentLoop 接入：** `create_tool_executor(adapter)` 注册四工具 handler，AgentLoop 通过 `ToolExecutor.execute()` 执行工具调用。  
- **缺口：** 权限/审计层未接入；ToolRegistry 仅用于元数据登记，未在执行路径中强制校验。

### 6.5 Security

- **Masker：** 手机 / 订单 / 身份证 / 银行卡 / 邮箱 / 门牌；`mask` + `restore`，支持 dict 链式调用。  
  - 正则优先级：邮箱 → 身份证 → 手机号 → **订单号（15-18位）→ 银行卡（16-19位）**，避免 16-18 位订单号被误识别为银行卡。  
- **全路径接入：**
  - Webhook：用户消息 mask 后进 LLM 和 DB
  - AnswerEngine：工具参数 restore 真实值 → 执行 → 工具结果 mask 后存入 session
  - DB 落库：user/assistant 消息均存脱敏版；发送给用户的回复 restore 还原
- **RBAC：** 角色（CUSTOMER / AGENT / ADMIN）+ 权限矩阵接口预留。

### 6.6 Common

- `Settings`：模型密钥、会话 debounce/timeout、抖店/淘宝、DB、Chroma。  
- `database`：`SessionRow` / `MessageRow` / `HandoffTask` / `BatchTestTask` / `BatchTestItem` ORM；启动时 `init_db()` 自动建表。  
- `logger`：structlog 结构化日志，lifespan 中初始化。
- **共享引擎：** `admin.py` 和 `batch_test.py` 均使用模块级 `_get_db()` 惰性单例，`session_store.py` 使用 `_ensure_db()` 延迟初始化，避免每个请求创建/销毁连接池。

---

## 7. API 一览

### 7.1 客服 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/api/qa` | 纯文本 FAQ 答疑 |
| POST | `/api/qa/multimodal` | multipart 多模态 / 多轮 |
| POST | `/webhooks/doudian` | 抖店消息推送回调 |
| POST | `/webhooks/taobao` | 淘宝千牛回调（兼容保留） |

### 7.2 管理后台 API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/admin/` | 管理后台 SPA 页面 |
| GET | `/admin/api/knowledge` | 知识点列表（支持集合筛选/分页/搜索） |
| POST | `/admin/api/knowledge` | 新增知识点（写入 ChromaDB） |
| PUT | `/admin/api/knowledge/{id}` | 更新知识点（先删后增） |
| DELETE | `/admin/api/knowledge/{id}` | 删除知识点 |
| POST | `/admin/api/knowledge/ingest` | 批量导入 JSONL |
| GET | `/admin/api/sessions` | 会话列表（分页） |
| GET | `/admin/api/sessions/{id}` | 会话详情（含消息） |
| POST | `/admin/api/sessions` | 录入历史会话记录 |
| DELETE | `/admin/api/sessions/{id}` | 删除会话 |
| GET | `/admin/api/stats` | 知识库数量 + 会话数量统计 |
| POST | `/admin/api/batch-test/upload` | 上传 Excel 批量测试 |
| POST | `/admin/api/batch-test/{id}/run` | 启动批量测试（后台异步） |
| GET | `/admin/api/batch-test` | 测试任务列表 |
| GET | `/admin/api/batch-test/{id}/status` | 测试任务进度 |
| GET | `/admin/api/batch-test/{id}/results` | 测试结果列表（分页/审核筛选） |
| PUT | `/admin/api/batch-test/items/{id}` | 审核单条测试结果 |
| DELETE | `/admin/api/batch-test/{id}` | 删除测试任务 |
| GET | `/admin/api/batch-test/template` | 下载 Excel 模板 |
| GET | `/admin/api/batch-test/{id}/export` | 导出测试结果为 Excel |

### 7.3 管理后台页面

| 页面 | 功能 |
|------|------|
| 仪表盘 | 知识库总数、集合分布、会话数、消息数 |
| 知识库管理 | 按 FAQ/商品/政策集合增删改查、批量导入 JSONL、ChromaDB 服务端搜索 |
| 会话记录 | 录入/查看/删除历史会话，按平台筛选 |
| 测试问答 | 输入问题 → 调用 `/api/qa` → 展示回答、命中状态、匹配来源、耗时、快捷 FAQ |
| 批量测试 | Excel 上传 → 后台批量问答 → 人工审核正确/错误 → 导出结果 |

启动：

```bash
# 推荐
python main.py
# 或
uvicorn apps.customer_service_agent.main:create_app --factory --host 0.0.0.0 --port 8000
```

CLI：

```bash
python -m apps.customer_service_agent.cli.qa "你们的尺码准吗"
python -m apps.customer_service_agent.cli.qa --interactive
```

管理后台：浏览器打开 `http://localhost:8000/admin/`

---

## 8. 配置（环境变量）

| 类别 | 变量前缀 | 说明 |
|------|----------|------|
| 应用 | `APP_*`, `LOG_LEVEL` | 环境、端口、日志 |
| 生成 LLM | `DEEPSEEK_*` | DeepSeek V4 Flash（RAG / 改写 / Agent） |
| 视觉 LLM | `MIMO_*` | MiMo-V2.5（截图理解 + OCR） |
| 向量 | `DASHSCOPE_*` | Embedding（text-embedding-v3） |
| 精排 | `JINA_*` | Jina Reranker v2 |
| 抖店 | `DOUDIAN_*` | 抖店开放平台（AppKey/AppSecret/ShopID/AccessToken） |
| 淘宝 | `TAOBAO_*` | 淘宝开放平台（兼容保留） |
| 数据 | `DATABASE_URL`, `CHROMA_PERSIST_DIR` | SQLite + ChromaDB |
| 会话 | debounce 500ms / vision 阈值 0.85 / timeout 30s | 见 `common.config` |

> **注意：** DeepSeek 模型须使用 `deepseek-v4-flash`（聊天）或 `deepseek-v4-pro`（推理）；`deepseek-chat` 和 `deepseek-reasoner` 已于 2026-07-24 停用。测试问答功能需同时配置 `DEEPSEEK_API_KEY` 和 `DASHSCOPE_API_KEY`。

---

## 9. 部署与运行

```bash
pip install -e ".[dev]"
cp .env.example .env   # 填入密钥

# 灌库（或通过管理后台在线录入）
python -m knowledge_platform.knowledge_service.ingest \
  --source data/faq.jsonl --collection kb_faq --clear

# 启动服务
python main.py

# 测试
python -m pytest tests/ -q   # 56 项全部通过
```

**多实例注意：** `ConversationManager` / EventBus / 部分 client 为进程内单例；生产需会话落库 + 共享存储。

---

## 10. 已知缺口与风险

| 风险 | 影响 | 建议 |
|------|------|------|
| 抖店 API 待真实验证 | 签名/调用逻辑已实现但未用真实 token 联调 | 申请抖店应用，用真实订单/商品验证 |
| Webhook 待真实推送 | 推送验签逻辑已实现但未验证 | 配置抖店消息推送地址，真实消息联调 |
| ToolCenter 权限/审计未接入 | 工具调用无权限校验、无审计日志 | ToolRegistry 与 ToolExecutor 深度集成，增加前置校验钩子 |
| AgentLoop vs ConversationManager 两套会话 | Webhook 走 AgentLoop，HTTP API 走 ConversationManager | 统一会话编排层 |
| `matched` 启发式回退 | JSON 解析失败时用简单关键词判断，可能边界误判 | 独立 judge 模型或更强结构化约束 |
| 淘宝适配器业务查询占位 | `query_order` / `query_logistics` / `query_product` 均返回 None | 如需接入，参照抖店适配器实现 TOP 签名 + API 调用 |
| 多实例水平扩展 | ConversationManager / EventBus / 部分单例为进程内 | 会话落共享存储（Redis/Postgres），EventBus 走 MQ |
| 知识库内存分页 | ChromaDB `get()` 不支持 offset/limit，Python 侧分页 | 知识库量级增大后考虑 Milvus/Qdrant 等支持分页的向量库 |

---

## 11. 演进蓝图

```
Channel Gateway (抖店 / 淘宝 / 拼多多 / 自建 IM)
        ↓
API Gateway + Auth/RBAC
        ↓
Agent Runtime Kernel (统一 run：Event · Session · Decision · Planner)
        ↓
   Multimodal │ Knowledge │ Tool Center │ LLM Router
        ↓
Infra: Postgres · Redis · MQ · OSS · Observability
```

**建议优先级：**

1. ~~打包与测试对齐新架构~~ ✅（2026-07-22）  
2. ~~清理空壳模块与兼容层~~ ✅（2026-07-31）  
3. ~~管理后台（知识/会话/测试问答/批量测试）~~ ✅（2026-07-31）  
4. ~~DeepSeek V4 模型升级 + Jina 解析修复 + Prompt 优化~~ ✅（2026-07-31）  
5. ~~会话持久化（落库 + 按 buyer/platform/shop 恢复 + TTL + LRU）~~ ✅（2026-07-31）  
6. ~~Masker 全路径接入（用户输入 + 工具结果 + LLM 上下文 + DB 落库）~~ ✅（2026-07-31）  
7. ~~AgentLoop 走 ToolExecutor~~ ✅（2026-07-31）  
8. ~~淘宝 Webhook 签名校验~~ ✅（2026-07-31）  
9. ~~DB 共享引擎（消除每个请求创建/销毁连接池）~~ ✅（2026-07-31）  
10. ~~脱敏正则优先级修正（订单号 vs 银行卡）~~ ✅（2026-07-31）  
11. ~~ChromaDB cosine 距离显式配置~~ ✅（2026-07-31）  
12. ~~ChromaStore 公共 CRUD API（消除 `_collections` 直接访问）~~ ✅（2026-07-31）  
13. 抖店真实 API 联调 + Webhook 验签验证  
14. ToolCenter 权限/审计接入  
15. 统一 AgentLoop 与 ConversationManager 会话编排  
16. 充实商品/政策知识与评测集  
17. 多平台 Adapter 与水平扩展  

---

## 12. 小结

项目当前结构为：

> **`apps`（业务）+ `runtime`（编排）+ `knowledge_platform`（知识）+ `tool_center`（工具）+ `security`（安全）+ `common`（基础设施）**

- **当下可跑：** FAQ 严格答疑（含语义等价匹配）、多模态编排、Agent 工具循环（通过 ToolExecutor）、抖店适配器（签名+API+验签）、淘宝适配器（验签已实现）、脱敏全路径接入（用户输入→工具结果→LLM 上下文→DB 落库）、会话持久化（按 buyer/platform/shop 恢复 + TTL + LRU）、灌库、管理后台（知识/会话/测试问答/批量测试/仪表盘 5 页面）、CLI、56 项测试、DB 共享引擎、ChromaStore 公共 CRUD API。  
- **结构就绪、待联调：** 抖店真实 API、Webhook 真实推送。  
- **下一里程碑：** 抖店真实联调 → ToolCenter 权限/审计 → 统一会话编排 → 多平台扩展。

本文档以**当前仓库真实目录与代码状态**为准；预留模块在状态列标为预留，避免与已实现能力混淆。
