# 电商客服 Agent 系统 — 方案架构

> 项目：`ecom-customer-agent`  
> 版本：0.1.0  
> 定位：面向淘宝 / 拼多多 / 抖店的智能客服 Agent（一期以淘宝 + FAQ 答疑为主）  
> 文档生成日期：2026-07-22

---

## 1. 项目概述

### 1.1 业务目标

构建一套**可多平台接入**的电商客服智能体，核心能力包括：

| 能力 | 说明 | 当前状态 |
|------|------|----------|
| FAQ / 政策 / 商品知识答疑 | 严格基于知识库 RAG，少幻觉 | ✅ 可用 |
| 多模态咨询（截图 + 文字） | Vision + OCR → 查询改写 → RAG | ✅ 可用 |
| 订单 / 物流 / 商品实时查询 | Function Calling + 平台适配器 | 🔶 工具与循环已通，平台 API 占位 |
| 转人工 | 关键词兜底 + LLM 工具调用 | ✅ 逻辑可用 |
| 千牛 / 平台 Webhook 接入 | 消息入 → Agent → 回复 | 🔶 结构就绪，签名与真实 API 待接 |
| 敏感信息脱敏 | 手机号 / 订单号 / 身份证等 | ✅ Masker 可用 |
| 长期记忆 / MCP / 复杂任务规划 | 企业级扩展 | ⬜ 接口预留 |

### 1.2 设计原则

1. **平台无关内核**：Agent 只依赖 `PlatformAdapter` 抽象，不感知淘宝/拼多多/抖店实现细节。  
2. **严格 RAG 优先**：答疑路径强制“只基于检索片段回答”，无匹配则转人工话术。  
3. **多模态事件驱动**：图片上传不立即强答；经 Vision/OCR 提取上下文后，由决策引擎决定 ANSWER / WAIT / BEST_EFFORT。  
4. **Provider 可替换**：LLM / VLM / Embedding / Rerank 均抽象为接口，便于降级与切换。  
5. **职责分层演进**：新逻辑收敛到 `app/agent_runtime/*`，旧路径通过兼容层 re-export，避免大爆炸重构。

---

## 2. 技术选型

| 层次 | 技术 | 用途 |
|------|------|------|
| 运行时 | Python ≥ 3.10 | 异步业务逻辑 |
| Web 框架 | FastAPI + Uvicorn | HTTP API / Webhook |
| 配置 | pydantic-settings + `.env` | 集中管理密钥与阈值 |
| 日志 | structlog | 结构化日志 |
| 关系库 | SQLAlchemy 2.x async + aiosqlite | 会话 / 消息 / 转人工任务（schema 已备） |
| 向量库 | ChromaDB（本地持久化） | FAQ / 商品 / 政策知识检索 |
| 生成 LLM | DeepSeek（`deepseek-chat`） | RAG 生成、查询改写、Agent 工具循环 |
| 视觉 LLM | 小米 MiMo-V2.5 | 截图理解、结构化 Vision、OCR |
| Embedding | 阿里云 DashScope `text-embedding-v3` | 知识写入与查询向量化 |
| Rerank | Jina Reranker v2 multilingual | 召回二次精排 |
| HTTP 客户端 | httpx + tenacity | 外部 API 调用与重试 |
| 测试 | pytest + pytest-asyncio | 冒烟 / 多模态 / 工具 / 脱敏等 |

---

## 3. 总体架构

```
                    ┌─────────────────────────────────────────┐
                    │           外部入口 / 渠道               │
                    │  HTTP API  │  淘宝千牛 Webhook  │  CLI  │
                    └────────────┬────────────┬───────────────┘
                                 │            │
                    ┌────────────▼────────────▼───────────────┐
                    │         Gateway (FastAPI app.main)      │
                    │  /health  /api/qa  /api/qa/multimodal   │
                    │  /webhooks/taobao                       │
                    └────────────┬────────────────────────────┘
                                 │
          ┌──────────────────────┼──────────────────────────┐
          │                      │                          │
          ▼                      ▼                          ▼
   ┌─────────────┐    ┌────────────────────┐    ┌──────────────────┐
   │  QAService  │    │ ConversationManager│    │    AgentLoop     │
   │  严格 FAQ   │    │  多模态会话编排    │    │  RAG+工具循环    │
   └──────┬──────┘    └─────────┬──────────┘    └────────┬─────────┘
          │                     │                        │
          │           ┌─────────┴──────────┐             │
          │           │ DecisionEngine     │             │
          │           │ ImageAnalyzer      │             │
          │           │ ContextBuilder     │             │
          │           └─────────┬──────────┘             │
          │                     │                        │
          └──────────┬──────────┴────────────┬───────────┘
                     ▼                       ▼
              ┌─────────────┐         ┌────────────────┐
              │ RAGPipeline │         │ Tools + Adapter│
              │ Chroma+Jina │         │ order/logistics│
              └──────┬──────┘         │ product/handoff│
                     │                └────────┬───────┘
                     ▼                         ▼
         ┌───────────────────┐      ┌────────────────────┐
         │ DeepSeek / MiMo   │      │ TaobaoAdapter 等   │
         │ DashScope Embed   │      │ (平台开放 API)     │
         └───────────────────┘      └────────────────────┘
                     │
         ┌───────────┴───────────┐
         │ data/chroma  │ SQLite │
         │ FAQ 向量库   │ 会话表 │
         └───────────────────────┘
```

### 3.1 逻辑分层

| 层 | 目录 | 职责 |
|----|------|------|
| 接入层 | `app/api/`、`app/cli/` | HTTP / Webhook / 本地调试入口 |
| 业务 Agent 层 | `app/agent/` | QAService、AgentLoop、Session、Tools |
| Runtime 内核 | `app/agent_runtime/` | 会话、决策、规划、工具注册、安全、RAG 存储、记忆 |
| 多模态层 | `app/multimodal/` | Vision / OCR / ImageAnalyzer |
| 检索层 | `app/retrieval/` | RAG 流水线、灌库脚本（store 兼容转发到 runtime） |
| 平台层 | `app/adapters/` | 平台消息与业务 API 适配 |
| 模型层 | `app/llm/` | LLM / VLM / Embedding / Rerank Provider |
| 数据层 | `app/models/`、`data/` | ORM、本地 DB、Chroma、FAQ JSONL |
| 基础设施 | `app/core/` | 配置、日志 |

---

## 4. 目录结构（核心）

```
ecom-customer-agent/
├── app/
│   ├── main.py                 # FastAPI 入口
│   ├── core/                   # config / logging
│   ├── api/                    # qa / qa_multimodal / webhooks
│   ├── agent/                  # QA、AgentLoop、Session、Tools
│   ├── agent_runtime/          # 企业级 Runtime（主演进方向）
│   │   ├── conversation/       # SessionManager / State / Event / ContextBuilder
│   │   ├── decision/           # DecisionEngine
│   │   ├── planner/            # TaskPlanner（预留）
│   │   ├── tools/              # Registry / Executor / MCP（预留）
│   │   ├── security/           # Masker / RBAC（RBAC 预留）
│   │   ├── rag/                # ChromaStore 实现
│   │   ├── memory/             # 长期记忆（预留）
│   │   └── llm/                # runtime 侧 LLM 封装（扩展中）
│   ├── conversation/           # → agent_runtime 兼容 re-export
│   ├── multimodal/             # Vision + OCR + ImageAnalyzer
│   ├── retrieval/              # RAGPipeline + ingest + chroma 兼容层
│   ├── adapters/               # PlatformAdapter + taobao
│   ├── llm/                    # deepseek / mimo / dashscope / jina
│   ├── models/                 # SQLAlchemy ORM
│   └── cli/                    # 本地答疑调试
├── data/
│   ├── faq.jsonl               # FAQ 语料
│   └── chroma/                 # 向量库持久化
├── tests/
├── pyproject.toml
├── .env.example
└── 方案架构.md                 # 本文档
```

---

## 5. 核心数据流

### 5.1 纯文本 FAQ 答疑（`POST /api/qa`）

```
用户问题
  → QAService.answer
  → RAGPipeline.retrieve(collections=["kb_faq"], top_k=10 → Jina top_n=3)
  → 组装严格 System Prompt（禁止编造）
  → DeepSeek 生成
  → { answer, matched, sources }
```

**策略要点：**

- 仅查 `kb_faq`（一期主场景）。  
- Prompt 约束：只许基于片段；无相关内容必须输出转人工话术。  
- `matched` 通过答案中是否含「无法回答 / 转人工」做启发式判定。

### 5.2 多模态会话（`POST /api/qa/multimodal`）

```
multipart: text? + image? + session_id?
  → ConversationManager
       ├─ ingest_image → 异步 ImageAnalyzer
       │     ├─ VisionService (MiMo) → page_type / focus / intent / confidence
       │     └─ OCRService (MiMo) → text_blocks → filter_ocr_text
       ├─ ingest_text  → 500ms debounce 合并
       └─ DecisionEngine
            ├─ has_text + has_image     → ANSWER
            ├─ image confidence > 0.85  → ANSWER
            ├─ 超时 + 有内容            → BEST_EFFORT
            ├─ 纯文本                   → ANSWER
            └─ 否则                     → WAIT
  → ContextBuilder.rewrite（文本 + Vision + OCR → 检索 query）
  → QAService.answer(rewritten_query)
  → EventBus: ANSWER_READY
```

**会话状态机：**

```
COLLECTING → PROCESSING → READY / WAITING_USER → ANSWERED
```

| 状态 | 含义 |
|------|------|
| `COLLECTING` | 收集用户输入 |
| `PROCESSING` | 分析图片或生成回答中 |
| `READY` | 输入完整，可回答 |
| `WAITING_USER` | 等待用户补充（常见于仅上传图且置信度不足） |
| `ANSWERED` | 已产出回复 |

### 5.3 平台 Webhook + 工具 Agent（`POST /webhooks/taobao`）

```
千牛 payload
  → TaobaoAdapter.parse_incoming → IncomingMessage
  → Session
  → AgentLoop.handle
       1. 关键词兜底转人工（投诉 / 差评 / 转人工…）
       2. RAG 召回（faq + product + policy）
       3. LLM + Tools 循环（最多 max_steps=4）
            tools: query_order / query_logistics / query_product / handoff_human
       4. 达到步数上限 → 强制 handoff
  → adapter.send_message 回复买家
```

### 5.4 知识灌库

```bash
python -m app.retrieval.ingest --source data/faq.jsonl --collection kb_faq [--clear]
```

- 输入 JSONL：`{question, answer, tags?}`  
- 向量化文本：`Q: {question}\nA: {answer}`  
- Embedding：DashScope，`text_type=document`  
- 集合：`kb_faq` / `kb_product` / `kb_policy`

---

## 6. 模块设计详解

### 6.1 Agent Runtime（`app/agent_runtime`）

按职责拆分，形成可演进的「企业级内核」：

| 子模块 | 关键类 | 说明 |
|--------|--------|------|
| conversation | `ConversationManager`, `ConversationState`, `EventBus`, `ContextBuilder` | 多轮会话编排、事件、查询改写 |
| decision | `DecisionEngine` | ANSWER / WAIT / BEST_EFFORT |
| planner | `TaskPlanner` | 复杂任务分解（当前默认单步 RAG） |
| tools | `ToolRegistry`, `ToolExecutor`, `MCPClient` | 工具注册执行；MCP 预留 |
| security | `Masker`, `RBACManager` | 脱敏已实现；RBAC 权限矩阵预留 |
| rag | `ChromaStore` | 向量读写与检索 |
| memory | `MemoryStore` | 跨会话记忆预留（隐私敏感，默认不做） |
| llm | provider 扩展位 | 与 `app/llm` 协作 |

兼容策略：`app/conversation/*`、`app/retrieval/chroma_store.py` 等通过 `from app.agent_runtime... import *` 保持旧 import 可用。

### 6.2 LLM Provider 抽象（`app/llm/base.py`）

```
LLMProvider     → DeepSeek：chat + tools + JSON mode
VLMProvider     → MiMo：analyze(images) 结构化视觉
EmbeddingProvider → DashScope：document / query 双模式
RerankProvider  → Jina：top_n 精排
```

统一消息模型：`Message`（支持 `content` 文本或 `content_parts` 多模态）、`ToolSpec`、`LLMResponse`。

### 6.3 RAG 流水线（`app/retrieval/rag.py`）

```
query
  → 多集合向量召回 top_k（默认 20/集合）
  → 合并 candidates
  → Jina rerank → top_n（默认 4；QA 路径 3）
  → 生成 prompt_block
```

### 6.4 多模态管线（`app/multimodal`）

```
ImagePart[]
  ├── VisionService → VisionContext
  │     page_type, focus_area, detected_text,
  │     user_annotation, possible_intent, confidence
  └── OCRService → OCRResult(text_blocks + category)
        → filter_ocr_text（滤掉 menu/footer/nav/button）
  → MultimodalContext
```

OCR 过滤优先级：噪声类丢弃 → title/content 保留 → other 与 vision 关键词重叠则保留。

### 6.5 平台适配器（`app/adapters`）

```python
class PlatformAdapter(ABC):
    send_message / parse_incoming
    query_order / query_logistics / query_product
```

统一 DTO：`IncomingMessage`、`OrderInfo`、`LogisticsInfo`、`ProductInfo`。  
一期：`TaobaoAdapter` 结构与日志占位；`_call_top` 待接入官方签名 SDK。  
二期扩展点：`pdd/`、`doudian/` 同构实现。

### 6.6 安全

**Masker（已实现）**

- 邮箱、身份证、手机号、银行卡、订单号、门牌号  
- `mask_text` / `mask_dict` + `restore`（会话内映射表）  
- 建议：工具入参与送入 LLM 的用户原文先脱敏，回写用户前再 restore

**RBAC（预留）**

| 角色 | 权限示例 |
|------|----------|
| customer | qa:ask, order:query_own |
| agent | 订单查询/更新、回复、转人工 |
| admin | 知识库管理、审计读取 |

### 6.7 数据模型（`app/models/database.py`）

| 表 | 用途 |
|----|------|
| `sessions` | 平台 / 店铺 / 买家会话 |
| `messages` | 会话消息（role/content/meta） |
| `handoff_tasks` | 转人工任务 pending/done |

当前 `ConversationManager` 默认进程内内存会话；ORM 与 `init_db()` 已就绪，多实例部署需落库 + 共享存储。

---

## 7. API 一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/api/qa` | 纯文本 FAQ：`{question}` → `{answer, matched, sources}` |
| POST | `/api/qa/multimodal` | multipart：`text` / `image` / `session_id` / `wait` |
| POST | `/webhooks/taobao` | 千牛消息回调（AgentLoop） |

CLI：

```bash
python -m app.cli.qa "你们的尺码准吗"
python -m app.cli.qa --interactive
```

---

## 8. 配置与外部依赖

来自 `.env` / `Settings` 的关键项：

| 类别 | 变量前缀 | 说明 |
|------|----------|------|
| 应用 | `APP_*`, `LOG_LEVEL` | 环境、端口、日志 |
| 生成 | `DEEPSEEK_*` | RAG / 改写 / Agent |
| 视觉 | `MIMO_*` | 截图理解 + OCR |
| 向量 | `DASHSCOPE_*` | Embedding |
| 精排 | `JINA_*` | Reranker |
| 淘宝 | `TAOBAO_*` | 开放平台与 Webhook |
| 数据 | `DATABASE_URL`, `CHROMA_PERSIST_DIR` | SQLite + Chroma |
| 会话策略 | `conversation_debounce_ms=500` | 消息合并窗口 |
| 视觉策略 | `vision_confidence_threshold=0.85` | 高置信直接答 |
| 超时 | `conversation_timeout_sec=30` | BEST_EFFORT |

---

## 9. 部署与运行视图

### 9.1 推荐本地启动

```bash
# 1. 安装
pip install -e ".[dev]"

# 2. 配置密钥
cp .env.example .env   # 填入 DeepSeek / DashScope / Jina / MiMo

# 3. 灌知识库
python -m app.retrieval.ingest --source data/faq.jsonl --collection kb_faq --clear

# 4. 启动 API
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 9.2 进程内组件关系

- FastAPI 单进程内：`ConversationManager`、`EventBus`、`ChromaStore`、LLM client 多为模块级单例。  
- 图片分析、debounce、回答生成使用 `asyncio.create_task`。  
- **多实例注意**：内存会话不共享；需将会话状态迁到 Redis/DB，事件总线改为消息队列。

### 9.3 生产演进建议

| 阶段 | 建议 |
|------|------|
| 一期 | 单实例 + SQLite + 本地 Chroma，FAQ + 多模态答疑 |
| 二期 | 淘宝 TOP 真实签名、会话落库、Webhook 验签 |
| 三期 | 拼多多/抖店适配器、Redis 会话、审计日志 |
| 四期 | 任务规划、MCP 外部工具、长期记忆（脱敏后）、A/B 评测 |

---

## 10. 决策与风险点

### 10.1 已做的关键设计决策

1. **答疑与工具 Agent 分流**  
   - FAQ：`QAService` 严格引用，低温度。  
   - 订单类：`AgentLoop` + tools，避免 FAQ 路径误答实时状态。

2. **多模态不“看图直接闲聊”**  
   - 图 → 结构化 context → 改写 query → 仍走知识库，降低幻觉与越权答复。

3. **Runtime 与兼容层双轨**  
   - 新代码进 `agent_runtime`，旧路径 re-export，降低迁移成本。

4. **OCR 与 Vision 并行 + 噪声过滤**  
   - 避免菜单/页脚污染向量检索 query。

### 10.2 当前缺口 / 风险

| 风险 | 影响 | 建议 |
|------|------|------|
| 淘宝 Adapter 为占位 | 线上无法真实查单/回消息 | 接入 TOP 签名与消息 API |
| 会话仅内存 | 重启丢失、无法水平扩展 | 接入 `SessionRow`/`MessageRow` 或 Redis |
| Webhook 未验签 | 安全风险 | 实现 `X-Taobao-Signature` 校验 |
| `matched` 启发式 | 边界话术可能误判 | 引入结构化 JSON 输出或独立 judge |
| Masker 未默认接入主路径 | 敏感字段可能进 LLM | 在 ContextBuilder / AgentLoop 入口统一脱敏 |
| 双份会话概念 | `agent.Session` vs `ConversationState` | 逐步统一到 runtime 状态模型 |
| 外部模型可用性 | DeepSeek/MiMo/Jina 故障 | 增加熔断、缓存、降级话术与备用 Provider |

---

## 11. 目标架构（演进蓝图）

```
                 ┌──────── Channel Gateway ────────┐
                 │ 淘宝 │ 拼多多 │ 抖店 │ 自建 IM │
                 └───────────────┬─────────────────┘
                                 │
                 ┌───────────────▼─────────────────┐
                 │     API Gateway + Auth/RBAC     │
                 └───────────────┬─────────────────┘
                                 │
         ┌───────────────────────▼───────────────────────┐
         │              Agent Runtime Kernel             │
         │  EventBus · SessionStore · Decision · Planner │
         │  ToolRegistry · Masker · Memory · Audit       │
         └─┬───────────┬───────────┬───────────┬─────────┘
           │           │           │           │
      Multimodal     RAG      Tool Executor   LLM Router
      Vision/OCR   Chroma+     Platform API   DeepSeek/
                   Rerank      MCP Servers    MiMo/Backup
           │           │           │           │
         ┌─▼───────────▼───────────▼───────────▼─┐
         │     Infra: Postgres · Redis · MQ · OSS  │
         └─────────────────────────────────────────┘
```

**演进优先级建议：**

1. 打通淘宝真实收发与订单查询（业务闭环）。  
2. 会话持久化 + Webhook 安全。  
3. Masker / 审计默认开启。  
4. 商品 / 政策知识集合充实 + 评测集（命中率 / 转人工率 / 幻觉率）。  
5. 多平台 Adapter 与水平扩展。

---

## 12. 小结

本项目已形成清晰的 **「接入层 → Agent/Runtime → RAG/多模态/工具 → 平台与模型 Provider」** 分层架构：

- **当下可用**：FAQ 严格答疑、多模态截图咨询、本地灌库、CLI 调试、脱敏与决策框架。  
- **结构就绪**：Agent 工具循环、平台抽象、ORM、Runtime 扩展接口。  
- **下一里程碑**：平台真实 API、会话持久化、安全验签与主路径脱敏落地。

本文档描述的是**基于当前代码的方案架构**，随模块落地可持续修订「状态」列与演进优先级。
